source "https://rubygems.org"

# Match CI Ruby version
ruby '3.2.9'

# Use github-pages so local builds mirror GitHub Pages environment.
# If you prefer to use plain Jekyll, replace with `gem 'jekyll'`.
gem "github-pages"
